name: Trigger on Main Branch Changes

on:
  push:
    branches:
      - main

jobs:
  trigger-curl:
    runs-on: ubuntu-latest
    container: curlimages/curl:latest

    steps:
      - name: Trigger Nimbus Gateway Refresh
        run: |
          curl -X POST "https://85.9.202.121.nip.io/refresh?token=CTgdcHwvfldcLUJbipHsfMK5p" -H "Content-Length: 0"
